# SFTP/deploy config bevat hostnaam, gebruikersnaam en pad naar een SSH key
.vscode/sftp.json

# Secrets (SMTP, API-keys e.d.), nooit in git
config/secrets.php

# Let's Encrypt validatie: tijdelijk en server-side (Plesk), hoort niet in git.
# De rest van .well-known (bv. security.txt) wordt wel getrackt en gedeployd.
.well-known/acme-challenge/

# OS / editor
.DS_Store
Thumbs.db

# Dependencies (indien later toegevoegd)
/vendor/
/node_modules/
